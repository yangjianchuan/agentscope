# AgentScope 二次开发内容

> 整理日期：2026-09-17  
> 整理范围：当前工作区中尚未提交的修改与新增文件。整理前共有 58 个已跟踪文件发生变化，另有 9 个未跟踪文件；本文档自身不计入该快照。

## 1. 改动概览

本轮二次开发主要围绕以下能力展开：

| 模块 | 主要内容 | 直接效果 |
| --- | --- | --- |
| Codex Skills 接入 | 扫描 `~/.codex/skills`、建立本地 Hub、同步技能库和工作区 | AgentScope 可以直接发现并使用当前用户安装的 Codex Skills |
| Skill 管理 | 增加启停、打开本地目录、只读删除保护 | Web UI 可控制 Skill 状态，并避免误删由本地目录镜像的 Skill |
| 自定义模型目录 | 按凭据请求 OpenAI 兼容接口的 `/models` | 自定义 `base_url` 下的模型可以动态出现在聊天、TTS、Embedding 和知识库配置中 |
| 模型默认参数 | 自动补充思考模式和推理强度 | 新选模型以及旧会话加载时可以获得统一默认参数 |
| 聊天输入增强 | 增加 `/skill` 选择器和 `@文件` 自动补全 | 可在输入框内选择 Skill、浏览工作区文件并插入相对路径引用 |
| 工作目录生效 | 将会话 `cwd` 传入 Shell、Glob、Grep 和权限上下文 | 工具默认在用户选择的项目目录执行，而不是固定使用系统工作区根目录 |
| 示例服务本地化 | Redis 改为 SQLite，增加 Windows 服务脚本 | 降低本地运行门槛，可独立管理前端和后端服务 |
| 格式化器与解析器 | 调整多 Agent 首次历史标记；撤回 PPT 组合形状递归解析 | 存在明确行为变化，需要在合并前确认产品预期 |
| 仓库清理 | 完善 `.gitignore` | 本地数据库、日志、缓存、截图和 Blob 数据不再进入 Git 状态 |

## 2. Codex Skills 接入

### 2.1 本地技能发现

新增 `examples/agent_service/skill_discovery.py`，实现以下能力：

- 从当前用户的 `~/.codex/skills` 目录递归查找 `SKILL.md`。
- 同时发现普通技能与 `.system` 等嵌套目录中的内置技能。
- 按规范化路径稳定排序，避免不同扫描顺序导致列表抖动。
- 解析 `SKILL.md` 的 Front Matter，读取 `name`、`description` 和正文。
- 跳过元数据不完整、无法读取或编码异常的技能。

### 2.2 本地 Skill Hub

新增 `CodexSkillHub`，Hub ID 为 `codex-local`：

- 将本地 Codex Skill 映射为 AgentScope `SkillCard`。
- 支持搜索、游标分页、详情读取和 ZIP 下载。
- ZIP 打包时排除 `__pycache__` 和 `*.pyc`。
- 通过 `get_local_directory()` 暴露由服务端控制的可信目录。
- 将镜像记录标记为不可删除，但仍允许启用和禁用。

### 2.3 技能库与工作区同步

新增两个适配类：

- `CodexSkillSQLStorage`：用户首次读取技能库时，将 Codex Skills 同步到 AgentScope 技能库；可新增、更新和清理已经失效的镜像记录，并保留已有记录的 ID、创建时间和启用状态。
- `CodexSkillLocalWorkspaceManager`：新工作区通过 `skill_paths` 初始化技能；旧工作区在首次获取时也会补充同步，重复调用依靠工作区哈希去重。

`examples/agent_service/main.py` 已接入上述实现，并将示例服务存储从 Redis 切换为：

```text
runtime/agentscope.db
```

同时在 Skill Hub 列表中注册 `Codex Local` 和原有 `ClawSkillHub`。

### 2.4 Skill 管理接口与界面

后端新增或调整：

| 接口 | 用途 |
| --- | --- |
| `GET /skill` | 返回 Skill 的启用状态、是否可打开目录、是否允许删除 |
| `GET /skill/{skill_id}` | 返回包含 `SKILL.md` 正文的 `SkillDetail` |
| `PATCH /skill/{skill_id}` | 启用或禁用已安装 Skill |
| `POST /skill/{skill_id}/open-folder` | 在服务端主机文件管理器中打开可信本地目录 |
| `DELETE /skill/{skill_id}` | 对只读镜像 Skill 返回 `403`，其他 Skill 保持原删除逻辑 |

前端 Skill 页面增加：

- 启用/禁用开关。
- 本地 Skill 的“打开文件夹”按钮。
- 只读 Skill 隐藏删除按钮。
- 禁用状态标识和样式。
- “添加到工作区”对话框仅允许选择已启用 Skill。

工作区安装接口也增加后端校验，禁用的 Skill 不会被加入工作区。

## 3. 自定义模型目录与默认参数

### 3.1 按凭据动态获取模型

新增 `src/agentscope/app/_service/_model_catalog.py`。当凭据类型为 `openai_credential`，且存在 `base_url` 与 `api_key` 时，服务端会请求：

```text
{base_url}/models
```

请求使用 Bearer Token、8 秒超时，并设置 `trust_env=False`。其返回结果被映射为 AgentScope 的模型卡片：

- Chat：所有具有 ID 的模型都可生成 `ModelCard`。
- TTS：根据 `supported_endpoint_types` 或模型名称中的 `tts`、`text-to-speech`、`cosyvoice` 等特征识别。
- Embedding：根据端点类型或 `embedding`、`embed`、`bge`、`e5`、`gte` 等名称特征识别。
- 已知 TTS/Embedding 模型复用本地卡片元数据。
- 未知 Embedding 模型必须提供明确向量维度，否则不发布，避免创建无效向量集合。

远端元数据可从顶层或 `capabilities`、`limits`、`metadata`、`model_info`、`top_provider` 中提取。Chat 模型缺少限制时使用：

- 上下文长度：`128000`
- 最大输出：`16384`

远端请求失败时回退到本地静态模型列表；远端请求成功但结果为空时，保留空结果，不再错误回退。

### 3.2 API 和前端联动

以下查询新增可选参数 `credential_id`：

- `GET /model/`
- `GET /tts-model/`
- `GET /embedding-model/`

知识库 Embedding 模型列表也会优先使用对应凭据的远端模型目录。

前端模型 API、凭据详情页、聊天模型 Hook、TTS Hook 均改为按具体凭据请求模型，避免同一 Provider 下不同 `base_url` 共用一份静态列表。

### 3.3 模型默认参数

新增 `examples/web_ui/frontend/src/utils/modelParameters.ts`：

- 模型支持 `thinking_enable` 时默认设为 `true`。
- `reasoning_effort` 枚举包含 `high` 时默认选择 `high`。
- 只补充缺失参数，不覆盖用户已经保存的配置。
- 新选择模型时立即写入默认参数。
- 加载旧会话时自动补齐参数，并静默回写会话配置。
- 模型选择按钮在名称后显示当前 `reasoning_effort`。
- 参数弹窗改为在整个选中模型对象变化时刷新，而不只监听模型名称。

## 4. 聊天输入增强

### 4.1 `/skill` 选择器

聊天输入框增加已安装 Skill 菜单：

- 输入 `/`、`/关键字` 或 `/skill 关键字` 时打开筛选列表。
- 也可通过输入框工具栏中的 Skill 图标主动打开。
- 支持按名称、显示名称、描述和标签搜索。
- 仅显示已启用 Skill。
- 显示 Skill 是否已经进入当前工作区。
- 选择尚未进入工作区的 Skill 时，先调用工作区安装接口，再插入 `/skill <name>`。
- 支持方向键、`Enter`、`Tab` 和 `Esc` 操作，并补充 ARIA 组合框属性。

### 4.2 `@文件` 引用

聊天输入框增加工作区文件补全：

- 输入 `@` 后调用工作区目录 API 获取文件和文件夹。
- 支持逐级进入目录、模糊过滤、文件夹优先排序和最多 100 条结果。
- 显示文件大小，并区分文件与文件夹图标。
- 路径包含空格时自动生成 `@"path with spaces"` 形式。
- 拒绝绝对路径、Windows 盘符以及包含 `.`、`..` 的目录查询。
- 支持方向键、`Enter`、`Tab`、`Esc`，并处理输入法组合输入状态。

该功能插入的是工作区相对路径引用，不等同于上传附件。

## 5. 会话工作目录正式生效

原 `SessionConfig.cwd` 只用于界面展示，本轮改为实际参与工具执行：

- 每轮聊天通过 Backend 将 `cwd` 解析成绝对路径。
- `PowerShell`/`Bash`、`Glob`、`Grep` 默认以该目录执行或搜索。
- 权限上下文同时保留系统工作区根目录和用户选择的项目目录。
- 系统提示明确告知 Agent 当前工作目录，并说明工作区根目录仅用于会话状态、Skills 和工具结果存储。
- 显式传入工具的路径仍按工具自身规则解析。

对应修改覆盖 `ChatService`、`get_toolkit()`、`WorkspaceBase`、`LocalWorkspace`、`Glob`、`Grep` 及相关测试。

## 6. 格式化器与文档解析行为变化

### 6.1 多 Agent 格式化器

Anthropic、DashScope、DeepSeek、Gemini、Moonshot、Ollama、OpenAI Chat、OpenAI Responses、Volcengine 和 xAI 的多 Agent 格式化逻辑统一调整：

- 遇到第一个 `agent_message` 分组后，立即将“首次 Agent 历史消息”标记为已消费。
- 即使该分组格式化结果为空，后续消息也不再被视为第一条历史消息。
- Anthropic 和 Gemini 中验证旧行为的测试已删除。

这属于协议输出语义变化，不只是代码整理，合并前应确认这是目标行为。

### 6.2 PPT 组合形状解析

PPT 解析器移除了 `_iter_shapes()` 递归遍历，恢复为只遍历 `slide.shapes`：

- 组合形状及嵌套组合形状中的文本将不再被提取。
- 对应的两个测试数据构造函数和两个行为测试已删除。

如果业务 PPT 经常使用组合文本框，这会形成可见回退，应单独确认是否保留。

## 7. Windows 本地服务脚本

新增根目录脚本：

| 文件 | 功能 |
| --- | --- |
| `backend.ps1` | 使用 `.venv\Scripts\python.exe` 启动示例后端，监听 `8000`，日志写入 `runtime/logs` |
| `frontend.ps1` | 使用 pnpm 启动 Vite 前端，监听 `5173`，日志写入 `runtime/frontend/logs` |
| `services.ps1` | 统一执行前后端的启动、停止、重启和状态查询 |

三个脚本均提供菜单模式和 `start`、`stop`、`restart`、`status` 参数。当前停止逻辑按监听端口查找 PID 并强制终止，没有校验该进程是否由脚本启动；共享开发机上需防止误停占用同一端口的其他程序。

## 8. `.gitignore` 完善

新增忽略范围：

- 本地环境文件：`.env.*`，但保留 `.env.example` 和 `.env.template`。
- Python 工具缓存：`.ruff_cache/`、`.pytype/`、`.pyright/`。
- 编辑器与系统文件：`.vscode/`、`Thumbs.db`、`Desktop.ini`。
- pnpm、覆盖率和浏览器测试产物：`.pnpm-store/`、`coverage/`、`playwright-report/`、`test-results/`。
- AgentScope 本地运行数据：`/runtime/`、`/blobs/`。

因此 SQLite 数据库、服务日志、UI 验证截图和本地 Blob 文件不再出现在 Git 未跟踪列表中。

## 9. 测试与验证结果

### 9.1 已通过

- 前端执行 `pnpm.cmd --filter frontend build` 成功，TypeScript 编译和 Vite 生产构建均通过。
- 后端定向测试共 `128 passed`，覆盖：
  - 远端模型目录。
  - Codex Skill 发现、同步和打包。
  - Skill Hub、启停、只读删除保护和打开目录。
  - 会话工作目录传递。
  - Toolkit 和 LocalWorkspace。
  - Anthropic、Gemini 格式化器现有测试。

### 9.2 环境限制

包含 `tests/rag_parser_test.py` 的扩展测试结果为 `145 passed, 31 failed`。31 个失败均发生在测试数据构造阶段，原因是当前 `.venv` 缺少以下可选依赖：

- `reportlab`
- `python-pptx`（导入名 `pptx`）
- `pandas`
- `python-docx`（导入名 `docx`）

因此当前环境尚未实际验证 PPT 解析器剩余测试，也不能据此判断解析器断言是否通过。

### 9.3 构建警告

- Vite 提示部分产物超过 500 kB，主入口压缩前约 3.2 MB。
- `mime-types` 引用的 Node `path` 模块被浏览器兼容层外置。
- Starlette 测试客户端出现 `anyio.abc.BlockingPortal` 弃用警告。

## 10. 提交前待处理事项

1. `examples/web_ui/{console.error(e.message` 是一个 0 字节异常文件，没有功能内容，建议确认后删除，不应随功能提交。
2. `git diff --check` 当前报告以下文件末尾多出空行：
   - `examples/agent_service/main.py`
   - `examples/web_ui/frontend/src/hooks/useAvailableModels.ts`
   - `src/agentscope/app/_router/_model.py`
3. Git 同时提示 5 个文件存在 CRLF 将被转换为 LF 的风险，应在提交前统一换行符，避免无意义差异。
4. `useModels.ts` 被压缩为非项目常用格式，虽然能编译，但建议运行前端格式化工具后再提交。
5. 需要确认是否接受 PPT 组合形状文本不再解析，以及空 `agent_message` 仍消耗首次历史标记这两项行为变化。
6. 示例服务直接运行 `main.py` 时要求 `runtime` 父目录存在；通过 `backend.ps1` 启动会先创建日志目录，从而间接创建该目录。
7. 打开 Skill 文件夹的动作发生在后端服务所在机器；远程部署时不会打开浏览器用户本机目录。
8. 远端模型目录目前只支持 `openai_credential`，并禁用环境代理继承；需要代理访问模型服务时应另行配置实现。

## 11. 建议的提交拆分

为便于审查和回退，建议至少拆分为以下提交：

1. `feat: discover and manage local Codex skills`
2. `feat: load models from OpenAI-compatible credentials`
3. `feat: add skill and file selectors to chat input`
4. `feat: apply session cwd to shell and search tools`
5. `chore: add Windows service scripts and ignore runtime artifacts`
6. 将格式化器语义变化与 PPT 解析回退分别独立提交，不与新增功能混在一起。

## 12. 主要文件索引

- Codex Skills：`examples/agent_service/skill_discovery.py`、`examples/agent_service/main.py`、`src/agentscope/app/_router/_skill.py`、`src/agentscope/app/hub/_skill/_base.py`。
- 模型目录：`src/agentscope/app/_service/_model_catalog.py`、模型/TTS/Embedding Router 与 Schema、`examples/web_ui/frontend/src/api/model.ts`。
- 聊天输入：`TextInput.tsx`、`ChatContent.tsx`、`ChatViewport.tsx`、中英文 i18n 文件。
- 工作目录：`_chat.py`、`_toolkit.py`、`_glob.py`、`_grep.py`、`workspace/_base.py`、`workspace/_local_workspace.py`。
- Skill 前端：`pages/skill/index.tsx`、`hooks/useSkills.ts`、`api/skill.ts`、`AddSkillDialog.tsx`。
- 模型参数：`utils/modelParameters.ts`、`LlmSelect.tsx`、`ModelParametersPopover.tsx`。
- 行为调整：10 个 Provider Formatter、`rag/_parser/_ppt.py` 及对应测试。
- 本地运行：`backend.ps1`、`frontend.ps1`、`services.ps1`、`.gitignore`。
